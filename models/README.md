# Models

The training pipeline writes fitted models and metrics here:

- `pd_model.joblib`
- `model_metrics.json`
- `model_metadata.json`

Model binaries are excluded from git so that the repository stays lightweight and reproducible.

